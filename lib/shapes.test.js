const { Triangle } = require("./shapes");
const { Circle } = require("./shapes");
const { Square } = require("./shapes");

describe("Triangle", () => {
  test("should have a certain property/method", () => {
    const triangle = new Triangle(/* params */);
  });
});

describe("Circle", () => {
  test("should have a certain property/method", () => {
    const Circle = new Circle(/* params */);
  });
});

describe("Square", () => {
  test("should have a certain property/method", () => {
    const Square = new Square(/* params */);
  });
});
